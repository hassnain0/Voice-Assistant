module.exports = {
  semi: false, // Remove semicolons if added automatically
  singleQuote: true, // Use single quotes instead of double
  tabWidth: 2, // Set indentation width
  useTabs: false, // Ensure spaces instead of tabs
  trailingComma: 'none', // Prevent trailing commas
  bracketSpacing: true, // Ensure spaces inside brackets
}
